let name = ['Hong', 'Park', 'Kim', 'Lee', 'Cho'];

for ( let nameCall of name ) {
    console.log(nameCall);
}

/* 아래 구문과 같습니다.
for ( let i = 0; i < name.length; i++) {
    console.log(name[i]);
} */

